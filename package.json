{
  "name": "@frontity/now",
  "version": "0.1.0",
  "license": "MIT",
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "nodemon --watch ./src -e ts --exec tsc ",
    "build": "tsc",
    "test": "tsc && jest",
    "prepublishOnly": "tsc"
  },
  "peerDependencies": {
    "@now/build-utils": "^0.9.4"
  },
  "dependencies": {
    "@now/node-bridge": "^1.2.3",
    "cross-spawn": "6.0.5",
    "get-port": "5.0.0",
    "promise-timeout": "1.3.0"
  },
  "devDependencies": {
    "@now/build-utils": "^0.9.4",
    "@types/cross-spawn": "^6.0.0",
    "@types/glob": "^7.1.1",
    "@types/promise-timeout": "1.3.0",
    "nodemon": "^1.19.1",
    "typescript": "3.5.2"
  }
}
